---
import BookCard from "@/components/BookCard.astro"
import type { CollectionEntry } from "astro:content"

type Props = {
  books: CollectionEntry<"books">[]
  showTags?: boolean
  showRating?: boolean
  showDescription?: boolean
}

const { books, showTags, showRating, showDescription } = Astro.props

// Description cards are taller, so fewer of them fit above the fold.
const eagerCount = showDescription ? 3 : 4
---

<ul>
  {books.map((book, i) => (
    <BookCard
      book={book}
      showTags={showTags}
      showRating={showRating}
      showDescription={showDescription}
      loading={i < eagerCount ? "eager" : "lazy"}
    />
  ))}
</ul>

<style>
  ul {
    display: flex;
    flex-direction: column;
    gap: var(--space-2xs);
  }
</style>
