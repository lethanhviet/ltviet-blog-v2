---
import type { BookEntry } from "@/lib/content"

type Props = { books: BookEntry[] }

const { books } = Astro.props

const tags = [...new Set(books.flatMap((book) => book.data.tags ?? []))].sort()
---

<bookshelf-tag-filter>
  <div
    data-filters
    role="group"
    aria-label="Filter books by tag"
    hidden={tags.length === 0}
  >
    <button type="button" data-filter-tag="all" data-pill aria-pressed="true">
      All
    </button>
    {tags.map((tag) => (
      <button type="button" data-filter-tag={tag} data-pill aria-pressed="false">
        {tag}
      </button>
    ))}
  </div>
  <slot />
</bookshelf-tag-filter>

<style>
  bookshelf-tag-filter {
    display: block;
  }

  [data-filters] {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3xs);
    margin-block-end: var(--space-s);
  }
</style>

<script>
  class BookshelfTagFilter extends HTMLElement {
    private controller?: AbortController

    connectedCallback() {
      this.controller?.abort()
      this.controller = new AbortController()
      const { signal } = this.controller
      const buttons = [
        ...this.querySelectorAll<HTMLButtonElement>(
          ":scope > [data-filters] > button",
        ),
      ]
      const items = [
        ...this.querySelectorAll<HTMLLIElement>(":scope > ul > li[data-tags]"),
      ]

      buttons.forEach((button) => {
        button.addEventListener(
          "click",
          () => {
            buttons.forEach((b) =>
              b.setAttribute("aria-pressed", String(b === button)),
            )
            const tag = button.dataset.filterTag
            items.forEach((item) => {
              const tags: string[] = JSON.parse(item.dataset.tags ?? "[]")
              item.hidden = tag !== "all" && !tags.includes(tag ?? "")
            })
          },
          { signal },
        )
      })
    }

    disconnectedCallback() {
      this.controller?.abort()
    }
  }

  if (!customElements.get("bookshelf-tag-filter")) {
    customElements.define("bookshelf-tag-filter", BookshelfTagFilter)
  }
</script>
